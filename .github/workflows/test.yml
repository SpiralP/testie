name: Rust

on: [push]

jobs:
  build_windows:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v1

      - name: Install stuff
        run: |
          choco install llvm
          
      - name: Test
        run: clang
      
